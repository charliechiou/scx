[package]
name = "scx_mysched"
version = "0.1.0"
edition = "2021"
license = "GPL-2.0-only"

[dependencies]
anyhow = "1"
libbpf-rs = "=0.26.2"

scx_utils = {
    path = "../../../rust/scx_utils"
}

[build-dependencies]
scx_cargo = {
    path = "../../../rust/scx_cargo"
}